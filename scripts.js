'use strict';

var greeting = alert('Hello there and welcome');

var userName = prompt('What is your name?');

console.log('the user entered ' + userName);

var returnName = alert('Ok ' + userName + ' lets take a look at a basic html layout');
